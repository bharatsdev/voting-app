  
version: "3"

services:
  vote:
    build: ./vote-app
    command: python VotingService.py
    volumes:
     - ./vote-app:/app
    ports:
      - "5000:80"

  redis:
    image: redis:alpine
    ports: ["6379"]
