apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "votinghelm.fullname" . }}-test-connection"
  labels:
    {{- include "votinghelm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "votinghelm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
